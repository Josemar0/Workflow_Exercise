on:
  push:
    branches: [main]

jobs:
  deployment:
    runs-on: ubuntu-latest
      steps:
	-name: compile
	  run: javac -d bin src/HelloWorld.java
	-name: Running
          run: java -cp bin helloWorld
